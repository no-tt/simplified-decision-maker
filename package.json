{
  "private": true,
  "name": "simplified-decision-maker",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "preact build",
    "dev": "preact watch",
    "lint": "eslint src"
  },
  "eslintConfig": {
    "extends": "preact",
    "ignorePatterns": [
      "build/"
    ]
  },
  "devDependencies": {
    "eslint": "^6.0.1",
    "eslint-config-preact": "^1.1.0",
    "identity-obj-proxy": "^3.0.0",
    "preact-cli": "^3.0.0-rc.6"
  },
  "dependencies": {
    "preact": "^10.2.0",
    "preact-i18n": "^1.4.2",
    "rough-notation": "^0.4.0"
  }
}
